name: CI

on:
  push:
    branches:
      - main
      - modernization
  pull_request:
    branches:
      - main
      - modernization

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: test-password
          MYSQL_DATABASE: cs_app_db
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -ptest-password"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
          --health-start-period=20s

    env:
      DB_URL: jdbc:mysql://127.0.0.1:3306/cs_app_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      DB_USERNAME: root
      DB_PASSWORD: test-password
      HIBERNATE_DDL_AUTO: create-drop
      SHOW_SQL: "false"
      JWT_SECRET: MDEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MDE=
      CORS_ALLOWED_ORIGINS: http://localhost:3000
      DEMO_DATA_ENABLED: "false"

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "17"
          cache: maven
          cache-dependency-path: Backend/pom.xml

      - name: Make Maven wrapper executable
        run: chmod +x Backend/mvnw

      - name: Test backend
        working-directory: Backend
        run: ./mvnw -B verify

  frontend:
    name: Frontend
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: Frontend/package-lock.json

      - name: Install dependencies
        working-directory: Frontend
        run: npm ci

      - name: Run frontend tests
        working-directory: Frontend
        env:
          CI: "true"
        run: npm test -- --watchAll=false --passWithNoTests

      - name: Build frontend
        working-directory: Frontend
        env:
          CI: "true"
          REACT_APP_API_URL: http://localhost:8080
        run: npm run build

  docker:
    name: Docker images
    runs-on: ubuntu-latest
    needs:
      - backend
      - frontend

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Build backend image
        run: docker build --tag coupon-backend:ci ./Backend

      - name: Build frontend image
        run: >
          docker build
          --build-arg REACT_APP_API_URL=http://localhost:8080
          --tag coupon-frontend:ci
          ./Frontend
